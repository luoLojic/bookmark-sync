# 更新日志

本文件记录面向使用者的变更。开发过程中的审计、偏离记录与方法论见
[HANDOFF.md](./HANDOFF.md)。

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。`0.x` 阶段远端数据格式
仍可能调整，升级前建议在设置页留一份历史快照。

## [未发布]

### 新增

- 仓库文档补全：完整的 README（功能 / Windows 编译 / 使用方法 / 使用建议 /
  故障排查 / 常见问题）、非商业许可证、本更新日志
- `.gitignore` 补充忽略扩展签名私钥（`*.pem` / `*.p12` / `*.pfx` / `*.key`）——
  自行打包 crx 时产出的 `.pem` 一旦泄露，他人即可签出被浏览器视为同一扩展的包
- `.gitattributes` 明确各类文件的行尾规则（`.bat` / `.cmd` / `.ps1` 用 CRLF，
  其余用 LF），避免 Windows 开发与 Linux CI 之间产生字节差异

### 移除

- `.env.example`：本扩展不使用任何构建期环境变量，该文件内容本身也如此声明，
  留着只会让人误以为存在基于 env 的配置机制

## [0.1.0] - 2026-07-30

首个功能完整的版本。

### 新增

- **三个显式操作**：同步（双向三方合并）、上传（本地覆盖云端）、下载（云端覆盖
  本地），后两者执行前弹确认并给出将丢失的条目数
- **两种远端后端**：WebDAV，以及 S3 兼容对象存储（自实现 SigV4 签名，
  已用 AWS 官方测试向量校验）
- **字段级三方合并**：标题 / 地址 / 所在文件夹各自独立判定，一台设备改标题、
  另一台改地址时两个改动都保留
- **删除保护**：某一侧删除数量同时超过阈值条数与该侧总数占比时中止，列出条目
  并要求确认（默认 10 条 / 10%，可调）
- **历史版本**：每次内容变化写一份完整快照，永不自动删除；列表读
  `history/index.json`，不依赖目录列举，因此在不支持列目录的 WebDAV 上同样可用
- **首次同步**：按 URL 与文件夹路径匹配去重合并，或选择单向覆盖
- **乐观并发**：支持 `If-Match` 时用 ETag；不支持则降级为写后校验并在界面明确提示
- **定时同步**与**浏览器启动时同步**（均默认关闭）
- **运行日志**：环形缓冲最近 2000 条，可导出；凭据、`Authorization` 头、
  URL 中的 userinfo 与查询串写入前一律脱敏
- **动态权限**：manifest 不含 `*://*/*`，仅在「测试连接」时申请你填写的那一个地址
- 界面文字全部经 `chrome.i18n`，内置简体中文；零运行时依赖，打包 37.6 KB

### 修复

- 设置页确认覆盖层在打开页面时即显示、且取消与确认按钮都无法关闭。
  根因是 `hidden` 属性只靠 UA 样式表的 `display: none` 生效，而
  `.overlay { display: flex }` 缺少对应的 `[hidden]` 覆盖，脚本设 `hidden` 同样被
  压过。两个样式表改为全局兜底 `[hidden] { display: none !important }`
- 日志脱敏遗漏 `JSON.stringify` 形式的键名（`"password":"..."`）——
  最容易触发的场景恰是调试时顺手打印整个配置对象
- 顺序合并会导致**永不收敛**：两台设备各自追加一条书签即无限乒乓，且每轮都判定
  「内容有变化」而写入历史快照。改为三方判定，只有真正调整过顺序的一侧作为骨架
- 计划应用时父子文件夹互换位置会崩溃（那一刻等于把文件夹移入自身子树）。
  移动操作改为按目标深度升序执行
- 合并阶段的父链循环会让环上节点从根不可达而被静默丢弃 —— 表现为整片书签消失
  而输出树本身仍合法。增加可达性检查与兜底挂载

### 已知限制

- 以下验收项需要真实浏览器与多台设备，尚未完成：48 小时双机定时同步观察、
  DevTools 网络节流实测、Edge 兼容性验证、容器化 WebDAV 往返测试
- 已验证的 S3 服务商仅 AWS S3 与 MinIO，其他兼容服务未实测
- 首次同步靠 URL 与路径匹配身份，若某条书签的地址在两台设备上已不同，
  会被视为两条。建议接入新设备时用「上传 / 以云端为准」而非依赖合并
