# 开发交接记录

> 规则：本文件中已经存在的内容只能读取，禁止修改、改写、删除或重排。任何后续交接信息只能追加到文件末尾，并应包含日期、提交号（如已有）和验证结果。

## 2026-07-29 · 接手基线与首次审计

- 接手前源码已原样保存在 Git 根提交 `ad3b149`，后续修复可与原始实现逐项对照。
- 已完整阅读《书签同步器-需求文档》v0.1 与《书签同步器-技术规划方案》v1.0；开发顺序按 M0 → M1 → M2…，M1 未达到测试门槛前不进入 M2。
- 原始验证：TypeScript 通过；ESLint 5 个错误；测试无法启动；构建无法完成。
- 确认的前序缺口：缺少 `src/background.ts`、图标、测试、CI 与依赖锁文件；界面使用的多数 i18n key 与语言包不一致；取消错误分类和映射整表覆盖违反方案不变量。
- 本轮优先修复 M0，不把未完成的同步逻辑伪装成可用功能。

## 2026-07-29 · 第二轮审计与 M0 收口

接手状态：前一位 agent 的改动全部为未提交的工作区修改（基线 `ad3b149` 之上）。本轮先审计再提交，未改动其已有交接内容。

验证结果（本轮修复后全部通过）：`typecheck` 通过；`lint` 无告警；`test` 30 例全过；`size` 打包 19.6 KB / 上限 200 KB。`domain/tree.ts` 行覆盖 100%、分支 98%，达到方案 7.1 门槛。

确认前一位 agent 已修好的问题：i18n key 与语言包对齐、`AbortedError` 改归 `TransientError`（方案 4 要点 4）、缺失的 `src/background.ts` 与图标生成、CI、依赖锁文件。

本轮新发现并修复的三处缺陷：

1. `src/ui/options/options.ts` 二次确认调用了语言包中不存在的 `confirmAgain`，两处重置流程的第二道弹窗会显示 `⟦confirmAgain⟧`。已补该 key，并让第二道弹窗改用原本孤立未被引用的 `confirmResetSyncTitle` / `confirmResetAllTitle` 作标题。
2. `src/platform/storage.ts` 的 `putMap()` 名为「整表替换」实为合并，函数名与语义相反，且无任何调用点。已删除该函数（INV-2 由 `mergeMap` 单一入口保证），同步调整对应单测。
3. `src/background.ts` 中 `remoteCounts: baseline ? null : null` 是恒为 null 的死表达式。已改为直接 `null` 并注明远端计数在 M3 接入。

测试补强：`test/ui/i18n.test.ts` 原先只扫描 HTML 标记，这正是缺陷 1 未被发现的原因。已扩展为同时扫描 UI 脚本中的 `t()` 字面量 key、`confirmDialog()` 实参与 `errors.ts` 的 `messageKey`，并逐项验证过删除任一 key 都会使对应用例失败（非空断言）。

遗留与后续里程碑衔接：
- 语言包中 `guardBodyLocal` / `guardBodyRemote` / `resultSynced` / `capDegradedWarning` / `permissionNeeded` 等约 40 个 key 尚无引用，属 M3–M6 预留，本轮不删。
- `background.ts` 对 sync / upload / download / preview / cancel / testConnection / history 类请求一律返回 `errNotReady`，符合「不把未完成功能伪装成可用」的约定。`StatusPayload.localCounts` 与 `remoteCounts` 目前恒为 null，分别待 M2、M3 接入。
- `src/domain/` 目前仅有 `tree.ts`，M1 的 diff / merge / order / plan / guard / hash / firstsync / guid 全部未开始，是下一步工作起点。
- 仓库 `.git` 属主为另一 Windows 账户，本机操作前需 `git config --global --add safe.directory C:/Users/HP/Desktop/book/bookmark-sync`。

## 2026-07-30 · M1 domain 核心完成

提交：`5d07563`（guid/hash/crypto）、`5534f7e`（diff）、`7f6e16c`（order）、`ee26958`（merge）、`df031f3`（plan）、`8c5a2af`（guard）、`3f7cd58`（firstsync）、`cf1af96`（顺序三方判定修正）。

验证：typecheck、lint 通过；213 例测试全过；domain 行覆盖 99.59%、分支 94.94%，达到方案 7.1 的 M1 门槛（95% / 90%）；打包 19.6 KB / 上限 200 KB。

方案 7.2 的 5 条属性测试全部落地：幂等、同值收敛、`apply(plan(a,b),a)=b`、计划有序、双设备收敛。第 5 条方案原本建议放到 M4，但它在纯函数层面就能验证（一次同步等价于 `T = merge(baseline, local, remote)` → `apply(plan(local,T))` → `baseline'=T`），不必等 engine，因此在 M1 就补齐，M1 验收条件得以按字面满足。

九个 domain 模块全部完成：tree、guid、hash、diff、merge、order、plan、guard、firstsync。另加 `platform/crypto.ts` 提供注入侧的 SHA-256 与随机源。

### 本轮最重要的发现：需求 6.3 的顺序规则会导致永不收敛

方案 7.2 第 5 条一写出来就抓到真缺陷。两台设备各自在同一文件夹追加一条书签（最常见的并发编辑）永不收敛：

```
第 1 轮  A=[a]      B=[b,a]   远端=[b,a]
第 2 轮  A=[a,b]    B=[b,a]   远端=[b,a]
第 3 轮  A=[a,b]    B=[b,a]   远端=[b,a]   ← 无限乒乓
```

根因是需求 6.3 规则 1「以本地的顺序为骨架」若无条件成立，每台设备每次同步都把对方的顺序改回自己的。副作用不止顺序抖动：每轮都判定「内容有变化」，于是每次定时同步都写一份历史快照，FR-14 的无变化短路永远不生效，远端 `history/` 无限膨胀。

已改为三方判定：拿本地顺序与基线比，只有真改过顺序的一侧才当骨架，两侧都改过时仍以本地为准；判断「改过」只比公共元素的相对顺序，故单纯新增或删除不算重排。这不是放宽需求 —— 反而让需求 6.3 自己写的「最终以先同步者的顺序为准，另一方的重排会在下次同步时被覆盖」第一次真正成立（原实现下后同步方永远不会采纳对方顺序，那句话不可能发生）。`OrderInput` 因此新增 `base` 字段。**若后续要修订需求文档，6.3 规则 1 应据此更新。**

### 其他对方案的偏离与补充（均已在代码注释与提交信息中说明）

1. `hash.ts` 把逻辑根标题替换为固定占位符后再哈希。「书签栏」在英文界面下是 "Bookmarks bar"，属设备本地界面标签；若参与哈希，两台语言不同的设备会永久互判「内容有变化」。
2. `merge.ts` 增加父链循环检测。方案未提及，但字段级 parentGuid 判定会真的造出环（本地把 A 移入 B、远端把 B 移入 A 时两个节点各自「只有一侧改」），环上节点从根不可达会被装配阶段静默丢弃 —— 表现为整片书签消失而输出树本身仍合法。兜底时沿各侧父链回溯所属逻辑根，避免条目在 bar 与 other 之间搬家。
3. `plan.ts` 的 `LocalOp.create` 用平铺 `type/title/url`，而非方案 3.3 写的 `node: Bookmark | Omit<Folder,'children'>`。两者同构，平铺不必构造没有 children 的假 Folder。
4. `platform/crypto.ts` 用纯 JS 同步实现 SHA-256 而非 `crypto.subtle.digest`。后者是异步的，采用它会让 `HashFn` 及 merge/commit 全链路变成 async。已与 WebCrypto 在 14 种长度（含 55/56/57、63/64/65 两组填充边界）、多字节 UTF-8 与 100KB 载荷上交叉校验。
5. `domain/guid.ts` 的随机源是必填参数，不提供 `Math.random` 默认值 —— ESLint 红线一规则拦下了默认参数，规则是对的：默认值会让不纯性在调用点不可见。

### 工作方法说明（后续里程碑建议延续）

每个模块都按「先写测试表再写实现」推进，实现后用变异测试验证测试不空转：手工改坏实现的关键分支，确认对应用例真的失败。本轮共验证 26 个变异体，全部被杀。两处因此改进了测试或实现：

- `hash.test.ts` 原先用字符串长度充当假哈希，节点在两棵根之间移动时总长度不变，漏掉该差异 —— 改为 FNV-1a。
- `plan.ts` 原先无条件钳制索引，变异测试显示它完全不可观测（`splice` 本身就钳制越界），属于假装提供保证的死代码 —— 改为「越界容忍 + 负数报错」并各有用例。

`merge.ts` 的循环打破最初只被 1 个定点用例杀死（随机森林几乎撞不出父链互换），为此补了一条定向属性：同一批文件夹在两侧各按随机嵌套组织。

### 下一步

M2 本地适配：`platform/bookmarks.ts`、逻辑根识别（含 `folderType` 校验）、映射表读写与批量策略实测、plan 应用器、FakeBookmarks。`domain/plan.ts` 的 `applyPlan` 是严格的纯参考实现（前置条件不满足即抛错），M2 的真实 applier 必须对齐它的语义；差别在于面对 `chrome.bookmarks` 时还需显式把越界索引钳到长度以内。

M2 验收要求对 900/120 树完成一次全量应用 < 5s，并把映射写入性能实测数据记录在 PR 描述中，据此定稿方案 3.1 的批量策略（默认先逐条同步写）。

## 2026-07-30 · M2–M7 完成，项目可交付

提交：`14c293c`（M2 本地适配）、`2bebc11`（M3 远端与弱网）、`ad89724`（M4 引擎与崩溃矩阵）、`91ef9c1`（M5+M6 界面接通与 S3）、本次（M7 硬化与发布材料）。

验证：typecheck、lint 通过；531 例测试全过；打包 37.0 KB / 上限 200 KB。覆盖率 domain 99.59%/95.12%、remote 98.83%、engine 97.66%、platform 93.45%。

### 各里程碑验收对照

- **M2**：900/120 全量应用实测「计划生成 7ms + 应用 7ms」，远低于 5s 预算；API 调用 1020 create + 122 getChildren（ChildCounts 缓存把 index 钳制所需的 getChildren 从每条一次降到每个父一次）。映射逐条写入 1020 次。**方案 3.1 / 9.4 的批量策略据此定稿：维持逐条同步写，不引入批量 + 预留映射机制。** 依据是 storage.local 写入约 1–2ms，1020 次约 1–2s，加上 1020 次 create 后仍在预算内。该估算基于 Node 环境的调用次数推算，真机耗时待验证。
- **M3**：10% 丢包 5 个种子、组合弱网 3 个种子全过；截断在 gzip 与明文两种编码下都被识别；两台设备同 ETag 提交只有一个成功。**未完成项：方案要求的「容器化 WebDAV 读写往返」本环境无 Docker，与 Edge 兼容性一并留待真机。**
- **M4**：崩溃矩阵 27 例 + 状态不变量 4 例全过；412 重试收敛、轮次用尽如实报错；降级模式的 nonce 校验有独立用例。
- **M5 / M6**：三操作、四种确认覆盖层、首次同步三选项、删除保护流程、设置页全分组、动态 host 权限、测试连接、历史列表/下载/刷新索引、日志导出、两种重置、alarms/startup、S3 + SigV4（官方测试向量校验）均已接通。
- **M7**：性能基准与脱敏审计已自动化并纳入 CI；README 与 PRIVACY.md 就位；图标由脚本生成。**未完成项：48 小时双机定时同步观察、Chrome DevTools 网络节流手工验证、Edge 兼容性 —— 这三项本质上需要真实浏览器与两台设备，无法在此环境完成。**

### 本轮发现并修复的缺陷

1. **`redact()` 漏掉 JSON 形式**（脱敏审计发现，NFR-9）。正则要求键名后紧跟 `:` 或 `=`，而 `JSON.stringify` 产出的是 `"password":"..."`，键名自带闭引号，因此整条漏过。最容易触发的场景恰恰是调试时顺手 `log(config)`。已在键名后加可选闭引号，并补了针对四种书写形式的单测。这条正是「脱敏不能只测 redact 单元、要跑真实同步搜日志」的价值所在。
2. **需求 3 的快照体积估算偏乐观**（性能基准发现）。实测 900/120 树、接近真实长度的标题与带查询串的 URL，明文 174KB 而非 ≈100KB —— 需求的估算隐含约 110 字节/条，实际约 190 字节/条。但 **gzip 后 15KB，优于需求预期的「各约 25KB」**，一次提交 4 次快照级传输合计约 45–60KB，传输目标反而达成。门禁因此盯住「单份 gzip < 25KB」这个可验证量，而非明文大小。

### 累计对方案/需求的偏离（含前几轮，供修订文档时对照）

1. **需求 6.3 顺序规则会导致永不收敛**（M1 发现，已修正为三方判定）。两台设备各自追加一条书签即无限乒乓，且每轮都判定「内容有变化」而写历史，FR-14 永远不生效。修正后需求 6.3 自己写的「另一方的重排会在下次同步时被覆盖」才第一次真正成立。**建议据此更新需求 6.3 规则 1。**
2. **方案 4.2「下载仍走 PUT 以保持历史线性」未采纳**（M4）。下载的目标树就是远端现状，内容按定义不变，照做会让每次点「下载」都往 `history/` 塞一份完全相同的快照，与 FR-14 直接冲突。改为走无变化短路，基线仍更新到远端版本。
3. **`hash.ts` 把逻辑根标题替换为占位符**（M1）。「书签栏」在英文界面下是 "Bookmarks bar"，属设备本地标签；若参与哈希，两台语言不同的设备会永久互判有变化。
4. **`merge.ts` 增加父链循环检测**（M1，方案未提及）。字段级 parentGuid 判定会真造出环，环上节点从根不可达会被装配阶段静默丢弃 —— 表现为整片书签消失而输出树仍合法。
5. **`plan.ts` 的 `LocalOp.create` 用平铺 type/title/url**，而非方案 3.3 的 `node:` 字段。两者同构。
6. **`plan.ts` 的 move 按目标深度升序**（M2 发现的真 bug）。父子互换时若先移子，那一刻父还在子里面，等于把文件夹移入自身子树，浏览器会拒绝。
7. **SHA-256 与 HMAC 自己同步实现**（M1 / M6）。`crypto.subtle` 是异步的，采用它会让 `HashFn` 及 merge/commit 全链路变成 async。已与 WebCrypto 及 AWS 官方向量交叉校验。
8. **写后校验保留重新 GET**（M4）。方案说「读回 ETag 或重新 GET」，但 WebDAV 的 ETag 不透明，只有把内容读回比哈希才能发现截断（NFR-4）。代价是每次提交多一次快照级传输，已计入性能基准。

### 工作方法（建议后续延续）

每个模块先写测试表再写实现，实现后用变异测试验证测试不空转：手工改坏关键分支，确认对应用例真的失败。累计验证 40+ 个变异体。有四次因此改进了测试或实现：`hash.test.ts` 的假哈希用长度导致跨根移动漏检、`plan.ts` 的索引钳制是不可观测的死代码、`merge.ts` 循环打破只被定点用例杀死（补了定向属性）、`commit.ts` 的写后校验与 nonce 校验原先无人覆盖。

变异测试脚本务必先断言「目标字符串已找到」再替换 —— 有一轮 4 个变异体因替换未生效而假装被杀，差点得出错误结论。

### 后续工作

按优先级：

1. 真机验收三项（48 小时双机观察、DevTools 节流、Edge 兼容性）。
2. 容器化 WebDAV 的集成测试（`bytemark/webdav`），补上 M3 未完成的验收项，尤其要覆盖真实服务器在 MKCOL 与 ETag 上的实现分歧。
3. Playwright 端到端（方案 7.1 第四层），当前 `test/e2e/` 下只有纯 Node 可跑的脱敏审计。
4. 若真机实测映射写入超预算，按方案 3.1 的退化路径引入批量 + 预留映射。

## 2026-07-30 · 用户实测反馈：设置页确认覆盖层缺 hidden 兜底

用户实际装载扩展后打开设置页，报告「打开后就显示这个，点取消和确认都无法关闭这个页面」，附截图为一个内容为空的确认覆盖层铺满页面。这是首个来自真实浏览器的缺陷报告，而全部 531 例自动化测试都是绿的。

根因在 CSS 而非逻辑：`hidden` 属性只是靠 UA 样式表的 `display: none` 生效，作者样式里任何显式 `display` 声明都会盖过它。`src/ui/options/options.css` 写了 `.overlay { display: flex }` 却没有对应的 `[hidden]` 覆盖，于是 `<div id="confirm" class="overlay" hidden>` 从打开页面那一刻就是可见的。按钮「点了没反应」是同一原因的表象 —— `closeConfirm()` 设的 `hidden = true` 照样被 `display: flex` 压过，TypeScript 逻辑本身从未出错。`popup.css` 当时有 `.overlay[hidden]` 这条窄覆盖，所以 popup 不受影响。

修复没有采用「给每个 display 规则补一条 `[hidden]`」的写法，而是在两个样式表顶部各加一条全局兜底：

```css
[hidden] { display: none !important; }
```

理由是这个缺陷的本质是「新增一条 display 规则就可能悄悄干掉一个 hidden」，逐个补窄规则治不了后续新增。`popup.css` 里 `#confirm-choices` 带 `class="choices"`、而 `.choices { display: grid }` 同样没有覆盖，是完全一样的潜伏缺陷，本轮一并消除。

测试补强：新增 `test/ui/hidden-attribute.test.ts`，断言 `src/ui` 下每个样式表都有裸 `[hidden]` 兜底且带 `!important`，并要求 `pages` 清单与实际存在的 CSS 文件完全一致 —— 将来新增页面若忘了纳入检查，这条会失败而不是静默放过。已用变异测试验证不空转：把全局兜底改回窄的 `.overlay[hidden]`，5 例中 4 例失败。

### 顺带修掉的一个门禁陷阱

`eslint.config.mjs` 的 `ignores` 只列了 `dist/`，漏了 `npm run zip` 产出的 `release/`。因为 `npm run verify` 把 `size` 放在最后，首次运行是干净的，但此后任何单独 `npm run lint` 都会去 lint 打包后的单行 bundle，报出 757 个错误。已补进 ignores。

### 方法论教训

这个缺陷属于「静态资源语义」类：类型系统、ESLint、Node 环境下的单测全都看不见它，因为它既不是类型错误也不是逻辑错误，只在浏览器把 CSS 层叠算出来的那一刻才存在。同类风险还有 CSP、manifest 字段、图标尺寸、`chrome.*` 的真实行为差异 —— 后续若再收到真机反馈，应优先怀疑这类「只在浏览器里成立」的约束，并且每修一处就把约束落成可自动检查的形式（本轮即 `hidden-attribute.test.ts`）。这也再次说明 HANDOFF 里一直挂着的「真机验收三项」不是形式主义。

验证：typecheck、lint 通过；536 例测试全过（新增 5 例）；打包 37.6 KB / 上限 200 KB。

## 2026-07-30 · 同源缺陷排查与静态契约护栏

上一条的缺陷暴露的不是一个 CSS 笔误，而是一整类「只在浏览器加载那一刻才成立、Node 下的 547 例测试结构上看不见」的约束。本轮据此把同类面全部排查了一遍，并把查到的约束固化成可自动检查的形式。

### 排查结果：未再发现同类真缺陷

逐项核对，均无问题：

- **`$()` id 契约**（风险最高）。`popup.ts` / `options.ts` 在模块顶层用 `$(id)` 构造 `el` 表，取不到就 `throw`。一个 id 写错的后果是整页空白、无任何提示，比覆盖层缺兜底更难定位。两页共 50 个 id 全部对得上；`options.html` 的 `history-table` 只在标记里用，属正常。
- **HTML 内部引用**：`label[for]`、`aria-labelledby` / `describedby` / `controls` 无悬空指向，无重复 id。
- **i18n 占位符**：131 个 key，凡消息体含 `$n` 的都声明了对应 `placeholders`，无缺声明、无空 content；`default_locale: zh_CN` 与 `_locales/` 下唯一目录一致；manifest 的 `__MSG_*__` 全部有定义。
- **调用点实参数量**：先前一版审计脚本按逗号计数，把 `t('countsLine', fmtCounts(a), fmtCounts(b))` 这类嵌套调用算错，报出 4 处「不匹配」。逐个回源码核对后确认全部正确，是脚本的计数缺陷，非代码缺陷。记此一笔，以免后来者重复怀疑。

### 新增 `test/ui/markup-contract.test.ts`（11 例）

覆盖上面这些「加载期才失败」的约束：脚本引用的 id 必存在于 HTML、id 不重复、`label[for]` 与 aria 引用不悬空、HTML 引用的 css/js 确实会进 dist、manifest 引用的文件都有构建来源、`default_locale` 与 `__MSG_*__` 落地、声明的三个权限都被代码真正用到（少声明运行时报错，多声明放大授权面且过审时会被问）。

顺带记下构建脚本的一处特性：`scripts/build.mjs` 拷贝 html/css 时用 `existsSync` 兜底，文件改名不会让构建失败，只会静默产出一个缺文件的包。上述「css/js 必须出现在 build.mjs 拷贝表 / esbuild 入口」两条断言正是为此。

已用变异测试验证不空转：改坏 id、改坏 `for`、改坏 aria、改名 css、改名 js 入口、改 `default_locale`、改 `__MSG_*__`、改图标尺寸、多声明 `tabs` 权限、注入重复 id，共 11 个变异体全部被杀（11/11）。

验证：typecheck、lint 通过；547 例测试全过（新增 11 例）；打包 37.6 KB / 上限 200 KB。

### 仍需真机验证的项目（无变化）

本轮全部工作都在静态层面。48 小时双机定时同步观察、DevTools 网络节流、Edge 兼容性、容器化 WebDAV 往返、映射逐条写入的真机耗时 —— 这五项依然只能在真实浏览器与设备上完成，是下一位接手者的首要工作。

## 2026-07-30 · 仓库文档与元数据补全，凭据泄露排查

本轮不动任何功能代码，只完善仓库对外的一面，并按用户要求排查是否泄露地址或密钥。

### 泄露排查结果：无凭据泄露

对全部纳入版本控制的文件做了扫描：

- **无** PEM / 私钥块、**无** 真实 `Authorization` 头、**无** 真实 AWS 密钥。`test/e2e/redaction.test.ts` 里的 `AKIAEXAMPLESECRETID` 是脱敏审计刻意选的假串（含 `EXAMPLE`，在 AWS 与 GitHub 的密钥扫描白名单内）。
- 源码与测试里出现的全部主机名都是保留测试域：`*.test`、`*.invalid`、`example.com`、`localhost`。没有任何真实服务器地址。
- 凭据形态的赋值全部集中在测试夹具与文档示例中，业务代码里没有硬编码。

两项**不是泄露但值得知道**的发现，本轮未擅自处理：

1. `package-lock.json` 的 293 处 `resolved` 全部指向 `https://registry.npmmirror.com`。不含任何机密，但会把构建绑死在该镜像上 —— 别人克隆仓库或 GitHub Actions 跑 `npm ci` 都会去请求 npmmirror。若要改为官方源，需在能访问 registry.npmjs.org 的网络下重新生成锁文件，故留给下一位处理。
2. Git 历史中的提交者邮箱 `2556351693@qq.com` 会随仓库公开而公开（另有前一位 agent 的 `Codex <codex@local>`）。改动它需要重写历史，属破坏性操作，未执行。

### 新增与修改的文件

- **`LICENSE`**（新增）：按用户要求「除商用外其他都可以」撰写的非商业许可证，中文为准、附英文译文。明确授予使用/复制/分发/修改/研究权，唯一限制是商业用途，并逐项列出何为商业用途、何为非商业（个人自用、教学、学术、非营利、无偿分发均在授权内）。含免责声明与「本软件会改动书签并写远端，请先备份」的特别提示。**这是自撰许可证而非标准许可证** —— 原打算用 PolyForm Noncommercial 1.0.0，但本环境无法访问 polyformproject.org 取得权威原文，凭记忆复写标准法律文本风险更大，故改为自撰。若日后要换成标准许可证，PolyForm Noncommercial 1.0.0（软件）或 CC BY-NC 4.0（非软件）是对应选择。
- **`README.md`**（大幅扩写，122 → 约 480 行）：功能表、Windows 编译（含前置要求与「无需 Python/VS 工具链」的说明）、加载/更新/卸载、七节使用方法、使用建议、13 条故障排查、6 条常见问题、远端布局、可靠性设计、开发、许可证。故障排查逐条对着 `_locales` 里真实的 `err*` 文案写，不凭空编错误信息。
- **`CHANGELOG.md`**（新增）：0.1.0 的功能清单、五项已修缺陷、已知限制（含四项真机验收未完成项）。
- **`.gitignore`**（重写）：最重要的补充是 **`*.pem` / `*.p12` / `*.pfx` / `*.key`** —— 用户问过能否打包 crx，而 `chrome://extensions` 的打包功能会同时产出 `.crx` 与作为**签名私钥**的 `.pem`，后者泄露即意味着他人可签出被浏览器视为同一扩展的包。原先只忽略了 `.crx`。另补 npm/yarn/pnpm 调试日志、`tmp-*.mjs`（本轮临时审计脚本正是这个形态）、`.eslintcache`、编辑器与系统垃圾文件。
- **`.gitattributes`**（重写）：原先只有三行。补齐各扩展名的显式行尾规则，其中 `.bat` / `.cmd` / `.ps1` **必须 CRLF**（否则 cmd.exe 解析出错），其余 LF；二进制类型标注 `binary` 防止行尾转换；`package-lock.json` 标 `linguist-generated` 并关闭 diff；`git archive` 导出时排除 `test/`、`.github/`、`HANDOFF.md`。统一 LF 对本项目不是形式主义 —— 仓库在 Windows 开发而 CI 在 ubuntu，行尾漂移会让内容哈希与体积门禁的结果随平台变化。
- **`package.json`**：补 `license`（`SEE LICENSE IN LICENSE`）、`repository`、`bugs` 字段。
- **`.env.example`**（删除）：内容本身就声明「本扩展不需要任何构建期环境变量」，无任何引用点，留着只会让人误以为存在基于 env 的配置机制。

### 一处文档与实现不符，已按实现修正文档

初稿 README 在「远端路径不存在」条目里写了「扩展只创建最后一级目录，不会递归创建」。核对 `src/remote/webdav.ts:92` 的 `ensureContainer()` 后确认相反：它对 basePath 的每一段加上 `history` **逐级 MKCOL**，并把 405/301/409 都当作「已存在」跳过。已改为「扩展会逐级创建各层目录，出现 404 说明地址本身不对」。同样按实现核准了僵死锁阈值（`STALE_SYNC_MS` = 10 分钟，初稿误写「几十秒」）、默认超时 30 秒、默认重试 5 次、默认间隔 30 分钟、删除保护 10 条/10%，以及设置页分组的真实名称（是「同步」而不是「同步策略」）。

写用户文档时凭印象填参数是很容易犯的错，且这类错误没有任何测试会拦 —— 后续增改 README 建议同样逐条回源码核对。

验证：typecheck、lint 通过；547 例测试全过；打包 37.6 KB / 上限 200 KB；README 的 15 处内部锚点与 5 处文件链接全部可解析。

## 2026-07-31 · 按两份外部审计报告逐条修复（12 组缺陷）

本轮输入是两份独立审计报告（`book-审计报告.md`、`审计报告.md`），要求「综合一下，有则改之，无则加勉」。两份报告的编号体系不同（一份用 C/H/M/L 分级，一份用 BUG-nn），有大量重叠项，下表按我实际提交的 12 个 commit 归并，括号内是两份报告各自的编号。

### 已修的 12 组

| commit | 缺陷 | 后果 |
|---|---|---|
| `790c5a0` | C-1 / C-2 / H-2 / M-3 | S3 GET 丢 body；远端文件不存在时反手清空本地；锁非原子；内存锁泄漏 |
| `5cdfe5f` | BUG-01 / H-1 | 换远端后旧基线仍被采信；能力缓存哨兵值让实例永久停在降级模式 |
| `922cf25` | H-4 | 首次同步「认亲」结果没写回映射与本地树，整棵树被删掉重建 |
| `e56fd20` | H-5 / H-6 | VERIFY 比的是远端自报哈希；快照只校验两层 |
| `fc2acfb` | M-1 | 原子提交点之后取消会打断 VERIFY，基线永远落不下来 |
| `8bf747b` | H-3 / M-10 / H-7 | 定时同步遇到需确认时无声终止；upload/download 不校验 `confirmed`；界面显示原始 i18n key |
| `1982321` | L-1 / L-2 / L-4 | `.err` 与 `.error` 类名不一致；结果摘要被状态刷新覆盖；文件夹被计入书签数 |
| `62d57d6` | BUG-13 / L-8 | 历史索引里的路径未校验 —— 服务端可诱导扩展带着凭据读任意路径 |
| `8401996` | H-8 / M-12 | 单条应用失败（如 URL 非法）让整轮同步永久卡死 |
| `70b26b4` | M-4 / M-8 / L-5 | 重建索引擦掉原有元数据；`mergeMap` 并发丢条目；`uploaded` 计数不实 |
| `0bae373` | L-7 | 打破父循环时把整棵子树打平到根 |
| `59e8ad3` | M-11 / L-6 | 远端目录被删后不再自愈；`getStatus` 这个只读操作会落盘映射 |

每一组都做了变异测试：把修复代码改回缺陷形态，确认对应的新测试变红。共新增 9 个测试文件、133 例测试（547 → 680）。

### 两处我没照审计的建议改，理由

**BUG-01（基线与远端错配）**：报告建议「检测到远端指纹变化就中断并让用户确认」。我改成基线按远端身份隔离 —— 新增 `src/shared/config.ts` 的 `remoteIdentity(cfg)` 生成指纹（WebDAV 取 `url + basePath`，S3 取 `endpoint + region + bucket + prefix + forcePathStyle`，**刻意不含凭据**，改密码不该逼用户重置同步状态），存进 `K.baselineRemote`，`src/engine/identity.ts` 的 `judgeIdentity` 判定 ok/adopt/mismatch。关键差别是**只拦双向同步**：upload / download 的目标树不来自基线，让它们继续可用，用户就有一条不必「重置全部」的重新绑定路径。`adopt` 分支是给老用户的 —— 升级前的基线没有指纹字段，不该一律当作错配。

**H-4（认亲结果未写回）**：报告的替代方案是「已有映射的条目跳过宽松匹配」。这条**不能照字面执行**：`readLocalTree` 本身就会给节点分配并**持久化** GUID，而 `getStatus` 会触发读树，所以用户点同步时「刚分配的 GUID」几乎总是空集，照做等于彻底关掉宽松匹配。我第一版正是这么写的（把 `assignedGuids` 一路穿到 `matchFirstSync`），被 `test/engine/concurrency.test.ts` 的「选择合并」用例抓住 —— 期望 `{bookmarks:2, folders:1}` 实得 `{bookmarks:3, folders:2}`。改为**排除同时出现在两棵树里的 GUID**：两边都有说明已经配对好了，不需要也不该再参与模糊匹配。

### 顺手做的结构性改动

审计最大的一条结构建议是「`background.ts` 517 行、零测试，应拆成可注入的纯函数」。本轮没有整体重构，但把其中四块判定逻辑抽成了独立可测的纯函数：`decideAttention`（错误 → confirm/record 通道 + 徽标）、`judgeIdentity`、`isCapsUsable`、`remoteIdentity`。另外新增 `test/shared/invariants.test.ts`，用静态扫描把三条红线钉死：INV-1 的基线唯一写入点、INV-3 的清理入口、`domain/` 纯度、能力缓存不得再用哨兵值、覆盖操作必须校验 `confirmed`。

`src/shared/errors.ts` 里**删掉**了 `TypeError → NetworkError` 的兜底分类。fetch 抛的 TypeError 在 `platform/http.ts` 已经分类过了，这层兜底只会把编程错误伪装成可重试的网络错误，反而掩盖真问题。

### 工作方式上的一个坑（务必记住）

工作副本是 **CRLF**。所有变异脚本、所有靠字符串匹配定位的批量编辑，都必须先 `replace(/\r\n/g, '\n')` 再匹配，并且**匹配失败要断言报错**，否则脚本会「成功」地什么都没改，而你以为变异体没被杀是测试的问题。本轮在这上面栽了三次（`tmp-mutate-h4.mjs` 报 MUTATION TARGET NOT FOUND、两次 python heredoc 的 AssertionError）。另外别用 `node -e` 写含 `\{` `\}` 和换行的正则一行流，会 `SyntaxError`，且失败时备份文件根本没生成、恢复步骤跟着 ENOENT —— 改用 python heredoc 加 `shutil.copyfile` 备份。

### 审计提到但本轮未处理的项（下一位接手者的清单）

按我判断的优先级排列：

1. **M-2 / BUG-07**：virtual-host 风格的 S3 请求没有对应的 host 权限，且同步路径从不主动申请权限 —— 这类配置会直接失败。
2. **M-9**：`FormatVersionTooNew` 的处理路径未实现，INV-3 的承诺（遇到更高版本格式不动本地）目前是空的。
3. **BUG-10**：首次创建远端文件时没有条件创建保护（缺 `If-None-Match: *`），两端同时首同步会互相覆盖。
4. **BUG-12**：历史索引并发写入丢条目 —— 与已修的 M-8 同类，但在远端侧。
5. **M-7 / BUG-08**：预览与执行各读一次远端，确认对话框里的数字可能与实际结果不一致。
6. **L-3 / UI-02**：确认覆盖层缺焦点管理、焦点陷阱与 Escape 关闭，`pendingResolve` 是单值的（并发确认会丢一个）。
7. **M-5 / M-6**：历史索引无上限增长（与「每次同步 ≤60 KB」的需求冲突）、映射逐条写入是 O(n²) 字节。两份报告都建议**先测量再决定**，不要凭感觉优化。
8. 零碎观察：WebDAV 用 `http://` 时没有警告；`detectIfMatch` 应把非 412 的 4xx 也当作「不支持条件写」；`logger.ts` 每次都刷整个 2000 行缓冲。
9. **D-1…D-6**：实现与两份规格文档的既有偏差（顺序骨架、download 不写版本、首同步 skipGuard、计数文案、映射 flush 策略、缺 Playwright / 容器化 WebDAV / 真实 S3 的 e2e 层）。这些是**文档该回写**，不是代码该改。
10. `background.ts` 的整体拆分与测试覆盖 —— 上面只抽走了四块纯函数，装配、路由、能力缓存、配置补丁、预览、历史、重置这些仍然一行未测。

验证：typecheck、lint 通过；680 例测试全过（45 个文件，新增 133 例）；`domain/` 覆盖率 99.61% 行 / 94.67% 分支（门禁 ≥95 / ≥90）；打包 41.1 KB / 上限 200 KB。覆盖率最低的仍是 `platform/storage.ts`（67.14% 行）。

真机验收项无变化，仍未完成：48 小时双机定时同步观察、DevTools 网络节流、Edge 兼容性、映射逐条写入的真机耗时。（容器化 WebDAV / 真实 S3 往返按用户「本机没装 docker，只需开发浏览器插件」的口径已不在活跃范围内。）
