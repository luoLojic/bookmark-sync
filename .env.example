# Copy this file to .env only when a future local build integration requires
# environment variables. The current browser extension does not require any
# build-time secrets; WebDAV/S3 credentials are entered in the options page
# and stored through the extension storage adapter.
