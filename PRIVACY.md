# 隐私说明

## 数据流向

本扩展只把书签数据发送到**你自己填写的那一个远端地址**（WebDAV 服务器或 S3
兼容对象存储）。没有任何其他网络请求：没有遥测、没有统计、没有崩溃上报、没有
更新检查、没有第三方服务。

作者无法访问你的书签，也无法访问你的远端地址与凭据。

## 收集与存储的数据

存在浏览器本地（`chrome.storage.local`）：

| 数据 | 用途 |
|---|---|
| 远端地址、用户名、密码 / Access Key、Secret Key | 连接你指定的远端 |
| 基线快照 | 上次同步成功时的书签内容，三方合并的公共祖先 |
| ID 映射表 | 浏览器书签 ID 与扩展分配的稳定 ID 的对应关系 |
| 同步状态、上次同步结果 | 界面展示与僵死检测 |
| 运行日志 | 最近 2000 条，可在设置页导出 |
| 设备名 | 仅写入历史快照，供你在历史列表里分辨来源设备 |

上传到你的远端：

| 数据 | 说明 |
|---|---|
| 书签标题、URL、文件夹结构与顺序 | 同步的正体 |
| 扩展分配的稳定 ID | 跨设备匹配条目所必需 |
| 设备名、写入时间、版本号、内容哈希 | 快照的信封字段 |

**不采集也不上传**：浏览历史、标签页、favicon、书签的添加时间、点击次数、
任何形式的用户标识。扩展只申请 `bookmarks`、`storage`、`alarms` 三项权限，
以及你填写的那一个远端地址的访问权限（动态申请，不使用 `*://*/*`）。

## 凭据的存储方式

远端凭据以**明文**存在 `chrome.storage.local`。

这是一个明确的取舍，不是疏漏。浏览器扩展没有安全的密钥保管处：加密所用的密钥
必须也存放在同一个地方，能读到密文的攻击者同样能读到密钥，因此加密只提供观感上
的安全，不提供实际防护。真正的边界是浏览器配置目录本身的文件系统权限。

设置页的密码框默认掩码显示，仅为防止旁人窥屏。

## 日志

日志只保留在本地，除你主动点击「导出日志」外不会离开设备。凭据、
`Authorization` 头、URL 中的 userinfo 与查询串在写入日志前会被替换为 `***`。
项目里有一项自动化审计：跑一次完整同步，逐项搜索日志中是否出现凭据。

## 历史快照

每次书签内容发生实际变化时，会在你的远端 `history/` 目录写入一份完整快照，
**永不自动删除**。这是刻意的设计 —— 它是误删后唯一的兜底。占用会随时间增长，
设置页显示历史总数与估算占用，由你自行判断是否手动清理。

## 删除数据

- 「重置同步状态」：清空基线，保留 ID 映射与配置
- 「完全重置」：清空基线、ID 映射与全部配置（含远端地址与凭据）
- 卸载扩展：浏览器会一并删除全部本地数据

以上操作都**不会**删除远端已上传的数据。远端数据需要你自行在 WebDAV 服务器或
对象存储上删除。
